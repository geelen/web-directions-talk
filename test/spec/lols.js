describe("Lols", function() {
  it("should be possible", function() {
    var eval, arguments;
    eval = 010;
    if (arguments) {
      function x() {};
    }
    delete eval;

    expect(eval).toBe(8);
  });
});
